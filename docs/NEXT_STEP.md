# Next Step

Status: **done** (awaiting next decision)

## Shipped

Progress tracking: `progress_total` / `progress_current` on `Execution`, `MoActions::Context` (`ctx.total=` / `ctx.progress`), create-as-`running` + `RunExecutionJob`, detail/index display with manual Refresh while running. Run again and unknown-total UX remain deferred.

## Next

_No slice decided. Ask to decide the next step._
